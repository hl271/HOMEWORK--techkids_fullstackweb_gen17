import React from 'react'

export default (props) => {
  return <button onClick={props.handleClick} id="addNewPlayerBtn" type="button" className="btn d-block mx-auto text-success border border-success"><i className="fas fa-plus"></i></button>

  
}
